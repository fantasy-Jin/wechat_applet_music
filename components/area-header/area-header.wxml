<!--components/area-header/area-header.wxml-->
<view class="area-header">
<view class="title">{{areaTitle}}</view>
  <view class="more" bindtap="onMoreClik" wx:if="{{hasMore}}">
    <text>更多</text>
    <van-icon name="arrow" />
   </view>
</view>
